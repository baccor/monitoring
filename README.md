AWS EKS monitoring stack. 

Features:
- Kube-prometheus stack
- Custom grafana dashboard


Optimized for t2.micro nodes(free tier).  
Node exporters are disabled to save pod capacity and resource limits are set to avoid OOMs and CPU exhaustion.